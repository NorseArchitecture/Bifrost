# Deliberately empty beyond the FROM. Everything is installed by features in
# devcontainer.json; this exists only so compose has something to build and tag,
# which is what lets the image be named vsc-bifrost instead of vsc-<hash>-features.
FROM mcr.microsoft.com/devcontainers/base:ubuntu
